pipeline {
    agent any
    stages {
        stage("Destroy API VM, dissociate any floating IP from it") {
            environment {
                FLOATINGIP = credentials("floating_IP")
                OS_PASSWORD = credentials("OS_PASSWORD")
            }
            steps {
                sh '''
                '''
            }
        }
        stage("Destroy the key pair") {

        }
    }
}